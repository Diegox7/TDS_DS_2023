var c = 32;
var f = 0;

function cgf(f, c) {
    //função para fazer o calculo
    var f = (c * 9/5) + 32;
    //tem que especificar o que você quer retornar no "return"
    return f;
}

//"console.log" é o que será exibido na tela
console.log(c + "° para fahrenheint é: " + cgf(f, c));


